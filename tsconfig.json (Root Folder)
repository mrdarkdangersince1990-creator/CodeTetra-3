/** @type {import('next').NextConfig} */
const nextConfig = {
  transpilePackages: ['three'],
  // This helps with 3D performance on Vercel
  compiler: {
    removeConsole: process.env.NODE_ENV === 'production',
  },
}

module.exports = nextConfig
